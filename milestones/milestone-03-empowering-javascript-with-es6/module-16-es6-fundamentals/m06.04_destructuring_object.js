// Destructuring for object

const students = {
    name: 'Alice',
    age: 23,
    roll: 13
};

console.log(students.name, students.age);       // Traditional
console.log();

const name = students.name;
const roll = students.roll;

console.log(name, roll);